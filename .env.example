# Optional. The app talks to the PHP API at same-origin /api by default,
# which is correct for production (trvlstory.reddevils.co.in serves
# both the static build and /api from one docroot).
#
# Only set this for local development when Vite (:5173) and PHP (:8080)
# run on different origins:
# VITE_API_BASE=http://localhost:8080/api
